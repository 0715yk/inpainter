export declare function getDrawCursor(strokeWidth: number, brushColor: string, strokeColor?: string): string;
export declare function dataURItoBlob(dataURI: string): Blob;
export declare function getContainSize(containerWidth: number, containerHeight: number, outputWidth: number, outputHeight: number): {
    width: number;
    height: number;
};
